#include <stdio.h>  // importando a biblioteca standart IO

int main (){ // método main
    printf("hello word \n"); // escrevendo na tela

    return 0;// retornando 0, mas pode tbm mudar o método pra void e não retornar nada
}
